// normal function
function add(first: number, second: number): number {
    return first + second;
}

// arrow function 

const func2 = (first: number, second: number) => first + second

const adMath = (x: number, y: number): void => {
    x + y
}