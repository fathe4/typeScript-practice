"use strict";
// normal function
function add(first, second) {
    return first + second;
}
// arrow function 
const func2 = (first, second) => first + second;
const adMath = (x, y) => {
    x + y;
};
